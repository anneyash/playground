<%= form_with(model: choose_answer, local: true) do |form| %>
  <% if choose_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(choose_answer.errors.count, "error") %> prohibited this choose_answer from being saved:</h2>

      <ul>
      <% choose_answer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
