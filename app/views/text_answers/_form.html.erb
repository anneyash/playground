<%= form_with(model: text_answer, local: true) do |form| %>
  <% if text_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(text_answer.errors.count, "error") %> prohibited this text_answer from being saved:</h2>

      <ul>
      <% text_answer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
