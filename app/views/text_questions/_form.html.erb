<%= form_with(model: text_question, local: true) do |form| %>
  <% if text_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(text_question.errors.count, "error") %> prohibited this text_question from being saved:</h2>

      <ul>
      <% text_question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
