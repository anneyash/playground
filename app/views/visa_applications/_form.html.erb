<%= form_with(model: visa_application, local: true) do |form| %>
  <% if visa_application.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(visa_application.errors.count, "error") %> prohibited this visa_application from being saved:</h2>

      <ul>
      <% visa_application.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
